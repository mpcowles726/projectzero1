

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    
    <title>Fantasy Football Blog</title>

    <!-- STYLESHEETS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
    <link rel="stylesheet" href="css/styles.css">

    <!-- VENDOR SCRIPTS -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.3/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/js/bootstrap.min.js"></script>

  </head>
  <body>
    <div class="container">
      <div class="row">
        <div class="col-md-8 col-md-offset-2">
          <h1> FANTASY FOOTBALL STUFF </h1>
          <hr/>
          <form id="newPostForm">
            <div class="form-group">
              <input name="title" class="form-control" id="postTitle" placeholder="Title">
              <input name="content" type="text" class="form-control" id="postContent" placeholder="Content">
            </div>
            <center>
              <button type="submit" class="btn btn-default" id="submitBtn">Submit</button>
              </center>
          </form>
          <hr/>
          <ul id="postContainer">
          
          <% for ( var i = posts.length-1; i >= 0; i --) { %>
          <li data-index=<%= posts[i]._id %> class='white'><%= posts[i].title %><br><%= posts[i].content %></li>
          <% } %>
          </ul>
         
        </div>
      </div>
    </div>
    <!-- APPLICATION SCRIPTS -->
    <script src="base.js"></script>

  </body>
</html>